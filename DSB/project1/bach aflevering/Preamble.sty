\usepackage[utf8]{inputenc}
\usepackage[danish,english]{babel}

\usepackage{minted}

%Specielle mat. karakterer
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{textcomp}
\usepackage{amsmath}


%Farver i tabeller %\rowcolor{}
\usepackage{colortbl}

%Gengiv billeder
\usepackage{graphicx}  
\usepackage{subcaption}

%Edit Kapitler og sectioner
\usepackage{tocloft}
\usepackage{titlesec}
    \setcounter{tocdepth}{5}
    \setcounter{secnumdepth}{5}

\titleformat{\chapter}
    {\HUGE \bfseries}                          % format af tallet
    {\thechapter} % \thechapter                % label 
    {20pt} % 20pt                              % indhak mellem tal og skrift
    {\HUGE \bfseries \vspace{-40pt}}           % skriftstørrelse og type

\titleformat{\section}
	{\Large \bfseries} 	                       % format af tallet
	{\thesection}	                           % label
	{5pt}             	                       % indhak mellem tal og skrift
	{\Large \vspace{0pt}}			                       % skriftstørrelse og type

\titleformat{\subsection}
	{\large \bfseries}                         % format af tallet
	{\thesubsection}                           % label
	{5pt}             			               % indhak mellem tal og skrift
	{\large \vspace{-15pt}}           			               % skriftstørrelse og type
	
\titleformat{\subsubsection}
	{\normalfont \bfseries}                    % format
	{}                         % label
	{0pt}             			               % indhak mellem tal og skrift
	{\normalfont \bfseries \vspace{-15pt}}           		   % skriftstørrelse og type

%Margen og cellemargen
\usepackage[top=3cm, bottom=3cm, left=2.5cm, right=2.5cm]{geometry}
    \pagestyle{plain}
\renewcommand{\arraystretch}{1.5}

%Dansk kommaer i matematik environment (ikke 1000-tals separator)
\usepackage{icomma}


%Figurtekst ændret udseende
\usepackage[font=small,labelfont=bf,listformat=simple]{caption}

%Figurnummerering afhænger ikke af kapitel
\usepackage{chngcntr}
\counterwithout{figure}{chapter}
\counterwithout{table}{chapter}

%SI-enheder: Nem måde at opskrive SI-enheder i math environment
\usepackage{siunitx}
	\sisetup{output-decimal-marker = {,}}
	\sisetup{exponent-product = {\cdot}}
	\sisetup{per-mode = fraction}

%Fjerne start indhakket i alle textbokse.
\parindent = 0pt
\parskip = 1em

%Enummerate og itemize laver nu mindre hspace
\usepackage{enumitem}

%Til sindsyge tabeller
\usepackage{multirow}
\usepackage{makecell}

%Referencer
%\usepackage[style=numeric-comp]{biblatex}
%\bibliography{_Referencer}
%\usepackage{csquotes}
\renewcommand{\bibname}{Referencer}

%Store ligninger
\newcommand\ddfrac[2]{\frac{\displaystyle #1}{\displaystyle #2}}

% cdot automatisk når man skriver *
\mathcode`\*="8000
{\catcode`\*\active\gdef*{\cdot}}

% Author TOC -JVA
\newcommand\autsection[2]{%
  \section[#1~/ {\normalfont\small\itshape#2}]{#1}}
  
\newcommand\autsubsection[2]{%
  \subsection[#1~/ {\normalfont\small\itshape#2}]{#1}}
  
\newcommand\autsubsubsection[2]{%
  \subsubsection[#1~/ {\normalfont\small\itshape#2}]{#1}}
  
\newcommand\autchapter[2]{%
  \chapter[#1~/ {\normalfont\small\itshape#2}]{#1}}
  
  % Ny kommando så man kan lave en chapter/section uden at der er tal foran
\newcommand{\mysection}[2]{
    \setcounter{chapter}{#1}
    \setcounter{section}{0}
    \section*{#2}
    \addcontentsline{toc}{section}{#2}
}
\newcommand{\mychapter}[2]{
    \setcounter{chapter}{#1}
    \setcounter{section}{0}
    \chapter*{#2}
    \addcontentsline{toc}{chapter}{#2}
}

%MATLAB kode i LaTeX
\usepackage[framed,numbered,useliterate]{mcode}

%Tabel-opsætning i LaTeX
\newcolumntype{L}[1]{>{\raggedright\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}

\usepackage{hyperref}

\newcommand{\uhref}[2]{\underline{\href{#1}{#2}}}

%Smarte referencer
\usepackage[danish]{cleveref}                   % {Type af reference}{Ental}{Flertal}
	\crefname{figure}{figur}{figurerne} 
	\crefname{equation}{ligning}{ligningerne}
	\crefname{table}{tabel}{tabellerne}
	\crefname{appendix}{bilag}{bilagene}
	\creflabelformat{equation}{#2#1#3}          % Ligningsnumre uden parentes omkring

\usepackage[section]{placeins}
\usepackage{graphicx}

\usepackage{pdflscape}